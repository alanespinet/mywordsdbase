import React, { Component } from 'react';

class DeleteButton extends Component {
  render(){
    return (
      <a className="delete-button" href="#">Delete Word</a>
    );
  }
}

export default DeleteButton;
